{
  "buildCommand": "cp prisma/schema.prod.prisma prisma/schema.prisma && npx prisma generate && npx prisma db push --accept-data-loss && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
